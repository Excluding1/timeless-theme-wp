# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# WordPress uploads (managed via wp-admin, not git)
uploads/

# Node (if ever needed for build tools)
node_modules/

# Playwright test artifacts
.playwright-mcp/

# Claude Code preview/test artifacts (not part of the theme)
.claude/preview/
# Local Claude Code permission grants (per-machine, not for repo)
.claude/settings.local.json
