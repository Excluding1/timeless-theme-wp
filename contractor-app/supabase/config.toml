# Supabase project config — Timeless Resurfacing contractor app (Phase 1: secure read path).
# The full canonical config is generated by `supabase init`; this minimal file pins the project ref
# + the Edge Functions' auth posture. Functions NOT listed default to verify_jwt = true (Phase 2 JWT).
project_id = "vyhtazylezuifxqlrnyw"

[functions.webhooks-sm8]
verify_jwt = false   # auth = SM8 shared secret (ServiceM8 cannot send a Supabase JWT)

[functions.sm8-get-job]
verify_jwt = false   # auth = ADMIN_PROBE_TOKEN (internal probe only)

[functions.reconcile]
verify_jwt = false   # auth = ADMIN_PROBE_TOKEN via pg_cron

[functions.messages-inbound]
verify_jwt = false   # auth = RELAY_WEBHOOK_SECRET (Make cannot send a Supabase JWT)
